build/
build-*/
cmake-build-*/
.cache/
*.o
*.a
*.so
*.dylib

# JUCE / Xcode
Builds/
JuceLibraryCode/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# macOS
.DS_Store

# Generated factory content (produced by tools/factory_gen at build time)
resources/factory/generated/

# Test scratch output
tests/output/

# Web build. `web/build.sh` fetches emscripten into node_modules and emits bud.js with the wasm
# inlined; both are generated, so neither belongs in the repository.
web/node_modules/
web/package.json
web/package-lock.json
web/bud.js
web/*.png

# The single-file bundles, folded from the sources above by web/bundle.mjs.
web/bud-standalone.html
web/bud-fragment.html
